using UnityEngine;

namespace Unity.LiveCapture.VirtualCamera
{
    /// <summary>
    /// Attribute that can be applied to a Vector2 property in order to display a dropdown for control mode.
    /// The reticle can be controlled from the device and optionally from the game view.
    /// </summary>
    class ReticleAttribute : PropertyAttribute
    {
    }
}
