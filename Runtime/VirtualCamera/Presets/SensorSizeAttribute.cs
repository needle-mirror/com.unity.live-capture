using UnityEngine;

namespace Unity.LiveCapture.VirtualCamera
{
    /// <summary>
    /// Attribute that can be applied to a Vector2 property in order to display a sensor size selector
    /// configurable through <see cref="SensorPresets"/>.
    /// </summary>
    class SensorSizeAttribute : PropertyAttribute
    {
    }
}
